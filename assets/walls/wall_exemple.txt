{
    "texters_names": (list of all the texter names in this file),
    (the name of this texter): [
        [
            [
                start<-(the start posision of the slice, top, 0 to 1),
                end<-(the end posision of the slice, bottom, 0 to 1),
                [
                    R,
                    G,
                    B
                ]<-(the colour of the slice, top, 0 to 255)
            ]<-(a slice of the vertical line) 
        ]<-(a slice of the texter) 
    ]<-(a list of 64 items with are all a vertical slice of the texture)
}
